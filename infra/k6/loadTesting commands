$body = @{
  clientKey = "user:test1"
  endpoint = "/api/products"
  algorithm = "TOKEN_BUCKET"
} | ConvertTo-Json -Compress

$body


Invoke-RestMethod -Uri "http://localhost:8080/api/v1/ratelimit/check" `
  -Method POST `
  -ContentType "application/json" `
  -Body $body

  --------------------------------------------------------

curl.exe http://localhost:8080/actuator/prometheus | findstr ratelimiter